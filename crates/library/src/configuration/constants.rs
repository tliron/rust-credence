/// `credence.yaml` configuration file name.
pub const CREDENCE_CONFIGURATION_FILE_NAME: &str = "credence.yaml";

/// `.coordinator` file name.
pub const DEFAULT_COORDINATOR_FILE_NAME: &str = ".coordinator";

/// `.credence` directory name.
pub const CREDENCE_DIRECTORY_NAME: &str = ".credence";

/// `.html` suffix.
pub const HTML_SUFFIX: &str = ".html";

/// `.html` suffix regex.
pub const HTML_SUFFIX_REGEX: &str = r"\.html$";

/// `.r` midfix.
pub const DEFAULT_RENDER_MIDFIX: &str = ".r";

/// Default annotations delimiter (Markdown fenced code block).
pub const DEFAULT_ANNOTATIONS_DELIMITER: &str = "```";

/// `default.jinja` template file name.
pub const DEFAULT_TEMPLATE: &str = "default.jinja";

/// Default listen name.
pub const DEFAULT_LISTEN_NAME: &str = "http";
